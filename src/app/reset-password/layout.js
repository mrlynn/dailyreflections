export const metadata = {
  title: 'Reset Password | AA Companion',
  description: 'Reset your password for AA Companion',
};

export default function ResetPasswordLayout({ children }) {
  return children;
}