export const metadata = {
  title: 'Forgot Password | AA Companion',
  description: 'Request a password reset link for your AA Companion account',
};

export default function ForgotPasswordLayout({ children }) {
  return children;
}